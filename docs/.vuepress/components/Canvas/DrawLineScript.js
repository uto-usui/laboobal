export class DrawLineScript {
  /**
   * constructor.
   *
   * @param target
   */
  constructor(target) {
    this.target = target

    this.init()
  }

  /**
   * init
   * load image
   */
  init() {
    console.log('🎉 init DrawLineScript')
  }
}

export default DrawLineScript
